# HIG差分レビューの完了記録

2026-09-08 16:39 JST、ユーザーの「残りの未完了作業を進めてください。制限気にせず、一気に作りきっちゃってください」により再開。

2026-09-08 18:18 JST、対象キューと追加候補の実装・検証を完了。

- 対象: 未確認8ページ・375既存ルールと追加検討3件。前回までの同日完了分は26ページ・558既存ルール。
- 開始時使用率5%。今回はユーザーの明示指示により、前回の使用率増加・残量による自主停止を適用しない。usage resetは使わない。
- 根拠品質のため、最初は最大3ページ・50ルールの小batchと補助担当1名を維持。長いページはsectionに分割し、全sectionの意味レビューが終わるまでpage全体のsource-bound判断を再承認しない。
- 公式HIGだけを一次資料とし、全文を保存しない。証拠断片19語以下、stable IDと既存の未コミット変更を保持。
- 作業対象はローカルの修正・再生成・検証。コミット・push・リリース・mergeは行わない。既存heartbeatはPAUSEDを維持。
- 現在の正本: `main` / `8ab19dc`を基準にした未コミット変更。有効ルール3,742件、deprecatedを含め3,750件。

## 実装結果

今回残っていた8ページ・375既存ルールの公式source traceと意味を再確認し、必要な修正を反映した。9月6日に検出した35ページ・975既存ルールは、先行分と合わせてすべて確認済み。これは対象キューの解消であり、全HIGの全命題を網羅したという主張ではない。

| 今回のページ | 再確認した既存ルール | 追加 |
|---|---:|---:|
| Apple Pay | 43 | 32 |
| File management | 34 | 0 |
| Generative AI | 57 | 8 |
| Gestures | 34 | 0 |
| Immersive experiences | 35 | 1 |
| Machine learning | 83 | 10 |
| Siri | 28 | 6 |
| Wallet | 61 | 8 |
| 先行レビューのTab bars / App icons補完 | — | 3 |
| 合計 | 375 | 68 |

- Finder Syncの機能例3件、没入スタイルの例4件、機械学習の説明・UI例5件をSHOULDからMAYへ補正。既存のMUST/MUST_NOTの強度変更はない。追加68件のうち、独立した必須・禁止要件は9件。
- 日英の不自然な定型文、条件・例外、ユーザーの選択、アクセシビリティ優先度、入力方式、パス種別ごとの画像仕様を修正。Walletの本人確認8件は原文のiPhone / iOS 16以降という対象に限定した。
- Apple Pay7件を現行段落へ明示的に再traceし、旧IDキーも維持。既存ルールの削除・廃止は行っていない。
- Generative AIの30候補は現行の連続した文単位で確認。Machine learningの39候補は完全な文ではなく、現行段落内の厳密な句の一致として証明した。両者を区別して記録している。
- 分割生成されたルールに個別のレビュー補正が適用されない生成処理を修正。個別の確認済みscopeをページ共通scopeより優先する検証も追加した。
- 確認中に見つかった重複は根拠に沿って判断した。Machine learningの同じセキュリティ要件は異なる節の検索文脈として保持。Walletの画像用途制約とテキスト提供経路は、別々の原文命令へ絞り込んだ。

## 検証と証跡

最終検証の集計は[completion-verification.json](audits/2026-09-08/completion-verification.json)、対象キューは[remaining-review-queue.json](audits/2026-09-08/remaining-review-queue.json)に保存済み。各ページのレビューJSONには全対象ID、前後の判断、公式URL・節・取得時刻・ハッシュ、補正理由を記録している。証拠断片は19語以下で、原文全文は保存していない。

追加分は[35件](audits/2026-09-08/coverage-additions-review.json)、[Generative AI 8件](audits/2026-09-08/generative-additions-review.json)、[3ページ17件](audits/2026-09-08/completion-additions-review.json)、[Wallet 8件](audits/2026-09-08/wallet-additions-review.json)で採否と重複判断を追える。

`npm run ci`は57テスト成功・警告0。172正本ファイルとID registryの隔離再抽出はbyte一致。固定12ケースに加え、追加68件について24件のページング・日本語・scope・source trace検索チェックが成功した。個別scopeとbatch scopeの競合・fallbackを抽出テストに追加し、scopeのキー順変更は許容・実際の端末変更は拒否することも隔離検証済み。`git diff --check`も成功。

## 留保と対象外

- Walletのサムネイルは本文が「square」と記載する一方、寸法表が幅60〜90pt・高さ90ptを許容する。表の数値を保持し、厳密な1:1比率を新しいMUSTとして追加しない。これはApple側の表現差として記録し、推測で解消しない。
- 保存済みinventoryのblockedは0、ルール未生成は16ページ。ルートとカテゴリ等15ページに加え`image-wells`を含む。具体的な一覧は[coverage report](../dist/reports/coverage.md)と最終集計に記録する。未取得内容を推定して埋めていない。
- 実機のアクセシビリティ・表示・入力、画像や動画の意味、SDKの実装・提供状況、実際の決済や本人確認、外部ポリシー・法的適合、App Review承認は未検証。
- 9月6日の全体差分を起点に、対象ページを9月6〜8日に照合した。今回、対象外137ページを含む全ナビゲーションの再探索は行っていない。`dist/reports/update-diff.*`の既存の8月2日レポートは保持し、現時点の未完了件数として使用しない。
- 検索payloadの比較結果と、Astraの回答品質・総トークン消費の比較は別物。後者の実測結果はなく、最適化の保証をしない。

コード・正本・生成物はローカルに保存済み。コミット、push、merge、リリースは未実施。公開リポジトリは`git@github.com:syou8929/HIG_Rule.git`。既存の未コミット変更も保持している。
